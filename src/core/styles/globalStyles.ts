import { StyleSheet } from 'react-native';

export const GlobalStyles = StyleSheet.create({
  grow: {
    flex: 1,
  },
  fillHeight: {
    minHeight: '100%',
  },
  relative: {
    position: 'relative',
  },
});
